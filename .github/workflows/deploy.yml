name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install, build 🔧
        env:
          REACT_APP_NP_API_KEY: ${{ secrets.REACT_APP_NP_API_KEY }}
          REACT_APP_NP_BASE_URL: ${{ secrets.REACT_APP_NP_BASE_URL }}
          REACT_APP_BACKEND_BASE_URL: ${{ secrets.REACT_APP_BACKEND_BASE_URL }}
        run: |
          npm install
          npm run build
      - name: Deploy 🚀
        env:
          REACT_APP_NP_API_KEY: ${{ secrets.REACT_APP_NP_API_KEY }}
          REACT_APP_NP_BASE_URL: ${{ secrets.REACT_APP_NP_BASE_URL }}
          REACT_APP_BACKEND_BASE_URL: ${{ secrets.REACT_APP_BACKEND_BASE_URL }}
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: build
